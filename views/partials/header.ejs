<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" href="/images/fisherman2.svg" type="image/svg" />
    <title><%= title || "Rodney Rants" %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <header>
      <div>
        <a href="/"
          ><img
            src="/images/fisherman1.svg"
            alt="Logo"
            class="logo"
            width="100"
            height="100"
        /></a>
        <h1><strong>Rodney Rants</strong></h1>
      </div>
      <nav>
        <% if (user) { %>
        <span>Welcome, <%= user.username %>!</span>
        <a href="/dashboard">Dashboard</a>
        <% if (user.isAdmin) { %>
        <a href="/admin">Admin Panel</a>
        <% } %>
        <a href="/chat">Chat</a>
        <a href="/profile/<%= user.username %>">Profile</a>
        <a href="/logout">Logout</a>
        <% } else { %>
        <a href="/login">Login</a>
        <a href="/signup">Sign Up</a>
        <% } %>
      </nav>
    </header>
    <div class="container"></div>
  </body>
</html>
